<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Khaatabook - Create Hisaab</title>
    <link rel="stylesheet" href="/public/css/style.css" />
    <link
      href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css"
      rel="stylesheet"
    />
    <script src="https://cdn.tailwindcss.com"></script>
  </head>

  <body>
    <main class="w-full min-h-screen bg-zinc-100 font-['Helvetica']">
      <!-- Navbar -->
      <nav
        class="flex flex-col sm:flex-row sm:justify-between px-5 sm:px-10 py-5"
      >
        <h3 class="text-2xl tracking-tight mb-3 sm:mb-0">Khaatabook</h3>
        <div class="navlinks flex flex-col sm:flex-row gap-3 sm:gap-5">
          <a class="tracking-tight hover:text-blue-500 transition" href="/"
            >Home</a
          >
          <a
            class="tracking-tight hover:text-blue-500 transition"
            href="/create"
            >Create New Hisaab</a
          >
        </div>
      </nav>

      <!-- Main content -->
      <div class="px-5 sm:px-10 hisaabs">
        <h3
          class="capitalize text-xl sm:text-2xl font-medium mb-5 mt-8 tracking-tight"
        >
          Create New Hisaab
        </h3>
        <form action="/createhisaab" method="post" class="flex flex-col">
          <!-- Title input -->
          <input
            name="title"
            class="text-md px-3 py-2 w-full sm:w-2/3 lg:w-1/2 mb-5 rounded-md bg-zinc-200 transition duration-300 ease-in-out focus:bg-white focus:ring-2 focus:ring-blue-500 focus:outline-none"
            type="text"
            placeholder="Shopping ka hisaab, ghar ka kharch..."
            required
          />

          <!-- Content textarea -->
          <textarea
            name="content"
            class="block w-full sm:w-2/3 lg:w-1/2 px-3 py-2 resize-none bg-zinc-200 rounded-md min-h-48 sm:min-h-96 transition duration-300 ease-in-out focus:bg-white focus:ring-2 focus:ring-blue-500 focus:outline-none"
            placeholder="Write your hisaab."
            required
          ></textarea>

          <!-- Submit button -->
          <input
            type="submit"
            class="block mt-5 px-5 py-3 bg-blue-500 text-white rounded-md w-full sm:w-1/3 lg:w-1/4 hover:bg-blue-600 transition-transform duration-300 ease-in-out transform hover:scale-105 active:scale-95"
            value="Create Hisaab"
          />
        </form>
      </div>
    </main>
  </body>
</html>
