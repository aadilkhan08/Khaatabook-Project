<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Khaatabook - View Hisaab</title>
    <link rel="stylesheet" href="/public/css/style.css" />
    <link
      href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css"
      rel="stylesheet"
    />
    <script src="https://cdn.tailwindcss.com"></script>
  </head>

  <body>
    <main class="w-full min-h-screen bg-zinc-100 font-['Helvetica']">
      <!-- Navbar -->
      <nav
        class="flex flex-col sm:flex-row sm:justify-between px-5 sm:px-10 py-5 transition duration-500 ease-in-out"
      >
        <h3 class="text-2xl tracking-tight mb-3 sm:mb-0">Khaatabook</h3>
        <div class="navlinks flex flex-col sm:flex-row gap-3 sm:gap-5">
          <a
            class="tracking-tight hover:text-blue-500 transition duration-300"
            href="/"
            >Home</a
          >
          <a
            class="tracking-tight hover:text-blue-500 transition duration-300"
            href="/create"
            >Create New Hisaab</a
          >
        </div>
      </nav>

      <!-- Main content -->
      <div class="px-5 sm:px-10">
        <h3 class="text-xl sm:text-2xl font-medium mb-5 mt-8 tracking-tight">
          Hisaab of
          <span class="text-blue-500"><%= filename.split(".")[0] %></span> date
        </h3>

        <!-- Data Display -->
        <p class="font-medium text-md sm:text-xl mb-10 text-justify">
          <%= data %>
        </p>

        <!-- Edit link with hover animation -->
        <a
          class="text-zinc-400 hover:text-blue-500 transition duration-300 ease-in-out transform hover:scale-105"
          href="/edit/<%= filename %>"
          >Edit this hisaab...</a
        >
      </div>
    </main>
  </body>
</html>
