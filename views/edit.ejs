<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Khaatabook - Edit Hisaab</title>
    <link rel="stylesheet" href="/public/css/style.css" />
    <link
      href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css"
      rel="stylesheet"
    />
    <script src="https://cdn.tailwindcss.com"></script>
  </head>

  <body>
    <main class="w-full min-h-screen bg-zinc-100 font-['Helvetica']">
      <!-- Navbar -->
      <nav
        class="flex flex-col sm:flex-row sm:justify-between px-5 sm:px-10 py-5"
      >
        <h3 class="text-2xl tracking-tight mb-3 sm:mb-0">Khaatabook</h3>
        <div class="navlinks flex flex-col sm:flex-row gap-3 sm:gap-5">
          <a class="tracking-tight hover:text-blue-500 transition" href="/"
            >Home</a
          >
          <a
            class="tracking-tight hover:text-blue-500 transition"
            href="/create"
            >Create New Hisaab</a
          >
        </div>
      </nav>

      <!-- Main content -->
      <div class="px-5 sm:px-10">
        <h3 class="text-xl sm:text-2xl font-medium mb-5 mt-8 tracking-tight">
          You're editing hisaab of
          <span class="text-blue-500"><%= filename.split(".")[0] %></span> date
        </h3>

        <!-- Form -->
        <form
          action="/update/<%= filename %>"
          method="post"
          class="flex flex-col"
        >
          <!-- Textarea -->
          <textarea
            name="content"
            class="w-full sm:w-3/4 lg:w-1/2 px-3 py-2 resize-none bg-zinc-200 rounded-md min-h-48 sm:min-h-96 outline-none"
            placeholder="Write your hisaab."
          >
                    <%= data %>
                </textarea
          >

          <!-- Submit button -->
          <input
            type="submit"
            class="mt-5 px-5 py-3 bg-blue-500 text-white rounded-md w-full sm:w-1/3 lg:w-1/4 hover:bg-blue-600 transition"
            value="Update Hisaab"
          />
        </form>
      </div>
    </main>
  </body>
</html>
